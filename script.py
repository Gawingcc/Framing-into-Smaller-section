import pandas as pd\n\n# Function to filter columns and compare beam weights\ndef filter_and_compare(dataframe, column_filters, beam_weight_col):\n    # Filtering the dataframe to only include specified columns\n    filtered_df = dataframe[column_filters]  # Column filters are specified in a list\n    # Compare beam weights\n    weight_comparison = filtered_df[beam_weight_col].diff()  # Using .diff() for comparison\n    filtered_df['Weight Comparison'] = weight_comparison\n    return filtered_df\n\n# Sample usage\ndf = pd.DataFrame({\n    'Beam ID': [1, 2, 3],\n    'Depth': [5, 10, 15],\n    'Beam Weight': [100, 150, 200]\n})\n\nfilter_columns = ['Beam ID', 'Beam Weight']  # Columns to filter\nresult = filter_and_compare(df, filter_columns, 'Beam Weight')\nprint(result)\n